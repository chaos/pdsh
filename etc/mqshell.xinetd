# default: on
# description: The mqshd server for running quadrics elan3 jobs.
service mqshell
{
	disable	= no
	socket_type		= stream
	wait			= no
	user			= root
#	log_on_success		+= USERID
#	log_on_failure 		+= USERID
	server			= /usr/sbin/in.mqshd
}
